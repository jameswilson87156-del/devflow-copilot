spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:devflow-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration

devflow:
  ai:
    provider: local-rule
    base-url: http://127.0.0.1:0
    api-key:
    model: local-rule-mvp
    protocol: chat-completions-compatible
    fallback-to-local: true
